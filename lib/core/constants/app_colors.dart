import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Video background
  static const Color appBackground = Color(0xFFF1EFEC);

  // Main white
  static const Color white = Color(0xFFFFFFFF);

  // Primary pink
  static const Color primary = Color(0xFFE84F78);

  // Light pink
  static const Color primaryLight = Color(0xFFFCE5EB);

  // Text
  static const Color textDark = Color(0xFF171717);
  static const Color textGrey = Color(0xFF6E6E6E);

  // Card
  static const Color cardBlack = Color(0xFF101218);

  // Purple image background from video
  static const Color imagePurple = Color(0xFF9B8CC8);

  // Green online dot
  static const Color online = Color(0xFF35C779);

  // Yellow reply indicator
  static const Color replyYellow = Color(0xFFF2C24B);

  // Badge background
  static const Color badgeBackground = Color(0xFF45464D);

  // Border
  static const Color border = Color(0xFFE4E0DD);

  // Bottom navigation
  static const Color navBackground = Colors.white;
}